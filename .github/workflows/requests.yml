name: Minifiers Requests Tests

on: [ push, pull_request ]

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install Dependencies
      run: npm install

    - name: Start Minifiers server
      run: npm start &

    - name: Run requests tests
      run: npm test
